function hideAndShow() {
    var input = document.getElementById("passwordInput");
    if (input.type === "password") {
        input.type = "text";
    } else {
        input.type = "password";
    }
}

function hideAndShow2() {
    var input = document.getElementById("passwordInput2");
    if (input.type === "password") {
        input.type = "text";
    } else {
        input.type = "password";
    }
}